# ── Strava API Credentials ──────────────────────────────────────────
# Create an app at https://www.strava.com/settings/api
# Set "Authorization Callback Domain" to "localhost"
STRAVA_CLIENT_ID=your_client_id
STRAVA_CLIENT_SECRET=your_client_secret

# ── OAuth Tokens (optional) ────────────────────────────────────────
# Leave blank — the `authenticate` MCP tool handles this automatically.
# Or paste tokens from the Strava API settings page for bootstrap.
STRAVA_ACCESS_TOKEN=
STRAVA_REFRESH_TOKEN=

# ── Server Config (optional — defaults shown) ──────────────────────
# STRAVA_MCP_HOST=127.0.0.1    # Bind address
# STRAVA_MCP_PORT=8001          # HTTP port
# STRAVA_MCP_DB=strava_mcp.db   # SQLite database path (tokens + cache)
