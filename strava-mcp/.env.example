# Strava API credentials — get these from https://www.strava.com/settings/api
STRAVA_CLIENT_ID=your_client_id
STRAVA_CLIENT_SECRET=your_client_secret

# After completing OAuth flow, these will be managed automatically.
# You can bootstrap with tokens from the Strava settings page (read-only scope),
# or leave blank and use the `authenticate` MCP tool to get full-scope tokens.
STRAVA_ACCESS_TOKEN=
STRAVA_REFRESH_TOKEN=
