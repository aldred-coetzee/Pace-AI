# ── Garmin Connect Credentials ──────────────────────────────────────
# Your Garmin Connect login email and password.
# Used only for initial authentication (garmin-mcp-login CLI).
# After first login, garth saves session tokens to GARTH_HOME.
GARMIN_EMAIL=your_email@example.com
GARMIN_PASSWORD=your_password

# ── Server Config (optional — defaults shown) ──────────────────────
# GARMIN_MCP_PORT=8003            # HTTP port
# GARTH_HOME=~/.garth             # Where garth stores session tokens
