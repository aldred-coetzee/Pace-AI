# ── Server Config (optional — defaults shown) ──────────────────────
# PACE_AI_HOST=127.0.0.1    # Bind address
# PACE_AI_PORT=8002          # HTTP port
# PACE_AI_DB=pace_ai.db      # SQLite database path (goals)

# ── LLM Eval (optional — only needed for --live-llm test mode) ────
# OPENROUTER_API_KEY=sk-or-...
# ANTHROPIC_API_KEY=sk-ant-...
